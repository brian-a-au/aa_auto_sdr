"""Project-wide CLI constants. Lives apart from `core/exceptions.py` so future
CLI defaults (worker counts, paths, etc.) accumulate here. v0.5 introduces the
batch-summary banner width."""

BANNER_WIDTH = 60
